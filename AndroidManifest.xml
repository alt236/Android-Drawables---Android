<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aws.apps.androidDrawables"
    android:versionCode="008"
    android:versionName="0.0.8" >

    <uses-sdk
        android:minSdkVersion="5"
        android:targetSdkVersion="17" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name="aws.apps.androidDrawables.activities.Main"
            android:label="@string/app_name"
            android:theme="@style/Theme.Sherlock.ForceOverflow" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="aws.apps.androidDrawables.services.ExportIntentService" />
    </application>

</manifest>